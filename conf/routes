# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET         /users/:id                                                                @controllers.UserController.read(id: Long)
POST        /users                                                                    @controllers.UserController.create
